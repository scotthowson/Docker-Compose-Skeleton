{
  "name": "nginx-proxy-manager",
  "title": "Nginx Proxy Manager",
  "description": "Nginx Proxy Manager with a web-based UI for managing reverse proxies, SSL certificates, and redirections",
  "category": "web",
  "icon": "🌐",
  "tags": ["web", "reverse-proxy", "ssl", "nginx", "proxy-manager"],
  "variables": [
    {
      "name": "HTTP_PORT",
      "label": "HTTP Port",
      "description": "Public HTTP port for proxied services",
      "default": "80",
      "required": false
    },
    {
      "name": "HTTPS_PORT",
      "label": "HTTPS Port",
      "description": "Public HTTPS port for proxied services",
      "default": "443",
      "required": false
    },
    {
      "name": "ADMIN_PORT",
      "label": "Admin UI Port",
      "description": "Port for the Nginx Proxy Manager admin web interface",
      "default": "81",
      "required": false
    }
  ]
}
